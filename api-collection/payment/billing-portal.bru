meta {
  name: Billing - portal
  type: http
  seq: 4
}

post {
  url: {{paymentUrl}}/api/billing/portal
  body: json
  auth: none
}

body:json {
  {
    "tenantId": "tenant_demo"
  }
}

tests {
  test("returns a portal url or 409 if not subscribed", function() {
    expect([200, 409]).to.include(res.status);
  });
}
