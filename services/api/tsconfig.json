{
  "$comment": "jsx enabled because @krispy/auth -> @krispy/email pulls React Email .tsx into this project's type graph (the API itself authors no JSX).",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "types": ["node"],
    "jsx": "react-jsx"
  },
  "include": ["src"]
}
